# 구구단 산성비 게임 - 설치 및 실행 가이드

## 필수 요구사항

이 게임을 실행하려면 **Node.js**가 필요합니다.

## Node.js 설치 방법

### 1. Node.js 다운로드 및 설치

1. [Node.js 공식 웹사이트](https://nodejs.org/)에 접속합니다
2. **LTS 버전** (권장 버전)을 다운로드합니다
3. 다운로드한 설치 파일을 실행하여 설치합니다
4. 설치 중 "Add to PATH" 옵션이 있으면 체크합니다
5. 설치 완료 후 컴퓨터를 재시작합니다 (필요한 경우)

### 2. 설치 확인

PowerShell 또는 명령 프롬프트를 열고 다음 명령어를 실행합니다:

```bash
node --version
npm --version
```

두 명령어 모두 버전 번호를 출력하면 정상적으로 설치된 것입니다.

## 프로젝트 실행 방법

### 1. 의존성 설치

프로젝트 폴더에서 다음 명령어를 실행합니다:

```bash
npm install
```

이 명령어는 `package.json`에 명시된 모든 패키지를 설치합니다.

### 2. 개발 서버 실행

```bash
npm run dev
```

서버가 시작되면 다음과 같은 메시지가 표시됩니다:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 3. 브라우저에서 확인

브라우저를 열고 `http://localhost:5173/` 주소로 접속합니다.

게임이 정상적으로 표시되면 성공입니다! 🎉

## 사용 가능한 명령어

- `npm run dev` - 개발 서버 실행 (핫 리로드 지원)
- `npm run build` - 프로덕션 빌드 생성
- `npm run preview` - 빌드된 파일 미리보기
- `npm run lint` - 코드 린팅 검사

## 문제 해결

### npm 명령어를 찾을 수 없다는 오류가 발생하는 경우

1. Node.js가 제대로 설치되었는지 확인합니다
2. 컴퓨터를 재시작합니다
3. PowerShell 또는 명령 프롬프트를 관리자 권한으로 실행합니다

### 포트가 이미 사용 중이라는 오류가 발생하는 경우

다른 프로그램이 5173 포트를 사용하고 있을 수 있습니다. 다음 중 하나를 시도합니다:

1. 해당 포트를 사용하는 프로그램을 종료합니다
2. Vite 설정에서 다른 포트를 사용하도록 변경합니다

### 패키지 설치 오류가 발생하는 경우

1. 인터넷 연결을 확인합니다
2. npm 캐시를 삭제합니다: `npm cache clean --force`
3. `node_modules` 폴더와 `package-lock.json` 파일을 삭제한 후 다시 `npm install`을 실행합니다

## 추가 도움말

문제가 계속되면 다음을 확인하세요:
- Node.js 버전: 16.x 이상 권장
- npm 버전: 8.x 이상 권장
- 운영체제: Windows 10/11, macOS, Linux 모두 지원

